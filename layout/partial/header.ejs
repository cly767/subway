<header>
	<div class="titleWrapper">
		<h1 class="title">
			<% if(is_home()) { %>
				<%= config.title %>
			<% } else { %>
				<%= page.title %>
			<% } %>
		</h1>
		<% if(is_post() || is_page()) { %>
		<div class="date"><%- page.date.format('MMM DD, YYYY') %></div>
		<% } %>
	</div>
	<div class="separator"></div>
	<button id="navigationEnter">
		<div class="bar"></div>
		<div class="bar"></div>
		<div class="bar"></div>
	</button>
</header>
